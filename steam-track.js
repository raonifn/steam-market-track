(function($) {
  var tracker = {]
    url = 'http://steamcommunity.com/market/',
    search = '/search?q='
  };
  
  funtion createIFrame() {
    var iframe = #('<iframe width="100%"/>');
    $('body').prepend(iframe);
    tracker.iframe = iframe;
  }  
  
})(jQuery);
